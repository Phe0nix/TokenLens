# TokenLens
TokenLens is a Chrome extension that scans live websites to extract design tokens including colors, typography, spacing, shadows, gradients, and CSS variables. Inspect any element, compare snapshots, get accessibility fixes and token suggestions, and export to CSS, SCSS, Tailwind, Figma, DTCG, or JSON.
